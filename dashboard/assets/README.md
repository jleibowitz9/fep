# assets

Drop the FEP wordmark here as **`fep-logo.png`** (or SVG exported to PNG).

The sidebar loads it automatically and falls back to a text mark if it is
missing, so nothing breaks either way. A transparent background works best,
since it sits on the midnight green rail. Roughly 120px wide is plenty.
