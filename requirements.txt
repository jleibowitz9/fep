# The engine, the ESPN pull, the chart, the stat pack and the dashboard server
# use only the standard library. These are needed for one optional extra.

# Google Sheets push (cli.py push --live) through a service account. Not needed
# for the CSV/paste path, and not the path this repo uses: key creation is
# blocked on this account, so the push goes through the Apps Script web app
# instead. See appsscript/README.md.
google-api-python-client>=2.100
google-auth>=2.20
