# Nothing. The engine, the ESPN pull, the chart, the stat pack, the dashboard
# server and the CMS push all use only the standard library.
#
# This used to list google-api-python-client and google-auth for a
# service-account transport. That transport could never run here anyway -- key
# creation is blocked on this account by an organization policy, which is why
# the Apps Script web app exists -- and it was removed in September 2026 along
# with the legacy Sheet writer it served. See appsscript/README.md.
